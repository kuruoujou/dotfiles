%alias s (ssh -Y %s)
%alias v (vim %s)
%alias p (sudo puppet agent --test --pluginsync)
%alias fusb (sudo chmod -R a+rwx /dev/bus/usb)
%alias newpass (openssl rand -base64 32)
%alias chmox (chmod +x %s)
%alias rmkey (ssh-keygen -f "/home/kuroshi/.ssh/known_hosts" -R %s)
%alias socks (ssh -f -N -D 1080 fgl.ramblingahoge.net)
%alias m (ssh -Y krypton firefox -url %s.rosen;)
%alias runningjobs (qstat -r | cut -d '.' -f1 | awk 'NR > 5 {printf \"%s \",\$0}')
